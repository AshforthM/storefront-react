import React from 'react'
import styled from 'styled-components';

export default function SideBar() {
  return (
    <SideBarStyles>SideBar</SideBarStyles>
  )
}

const SideBarStyles = styled.div`
  background-color: wheat;
`;